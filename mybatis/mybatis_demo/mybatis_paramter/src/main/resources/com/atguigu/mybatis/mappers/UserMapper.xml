<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.mybatis.mappers.UserMapper">

<!--    User getUserById(@Param("id") int id);-->
    <select id="getUserById" resultType="User">
        select * from t_user where id=${id}
    </select>

<!--    List<User> getAllUser()-->
    <select id="getAllUser" resultType="User">
        select * from t_user
    </select>

<!--    User getUserByUsername(@Param("username") String username);-->
    <select id="getUserByUsername" resultType="User">
        select * from t_user where username = #{username}
    </select>

<!--    User checkLogin(String username, String password);-->
    <select id="checkLogin" resultType="User">
        select * from t_user where username = #{username} and password = #{password}
    </select>

<!--    User checkLoginByMap(@Param("map") Map map);-->
    <select id="checkLoginByMap" resultType="User" >
        select * from t_user where username = #{username} and password = #{password}
    </select>

<!--    User CheckLoginByUser(User user);-->
    <select id="CheckLoginByUser" parameterType="User" resultType="User">
        select * from t_user where username = #{username} and password = #{password}
    </select>

<!--    int getCount();-->
    <select id="getCount" resultType="int">
        select count(id) from t_user
    </select>

<!--    Map<String, Object> getUserToMap(@Param("id") int id);-->
    <select id="getUserToMap" resultType="map">
        select * from t_user where id = #{id}
    </select>
<!--    List<Map<String, Object>> getAllUserToList();-->
    <select id="getAllUserToList" resultType="map">
        select * from t_user
    </select>
<!--    Map<String, Object> getAllUserToMap();-->
    <select id="getAllUserToMap" resultType="Map">
        select * from t_user
    </select>

<!--    List<User> getAllUserLike(@Param("keyword") String keyword);-->
    <select id="getAllUserLike" resultType="User">
        select * from t_user where username like "%"#{keyword}"%"
    </select>

<!--    int deleteUsers(@Param("ids") String ids);-->
    <select id="deleteUsers" >
        delete from t_user where id in (${ids})
    </select>

<!--    List<User> getAllUserFromDynamicTable(@Param(tablename) String tablename);-->
    <select id="getAllUserFromDynamicTable" resultType="User">
        select * from ${tablename}
    </select>

<!--    Void insertUserGetKey(@Param("user") User user);-->
    <insert id="insertUserGetKey" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        insert into t_user values(null, #{username}, #{password},#{age}, #{gender}, #{email})
    </insert>
</mapper>